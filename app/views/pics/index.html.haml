.col-md-6.col-md-offset-3
	#pics.transition-enabled
		- @pics.each do |pic|
			.box.panel.panel-default
				= link_to (image_tag pic.image.url), pic
				%h2= link_to pic.title, pic 

- if user_signed_in?
	= link_to "Gram It", new_pic_path
